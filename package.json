{
  "name": "weather",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "live": "live-server . --port=5500 --quiet",
    "build": "sass --watch css/styles.scss:css/styles.css --sourcemap=none",
    "start": "yarn live"
  },
  "devDependencies": {
    "dart-sass": "^1.25.0",
    "live-server": "^1.2.1"
  },
  "dependencies": {}
}
